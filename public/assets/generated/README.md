# Generated visual assets

This folder is reserved for final project-bound image assets generated with `gpt-image-2` or manually supplied by designers.

The app can run without these bitmaps because it has code-native fallback visuals. When final images are generated, save them here with the filenames listed in `src/content/visualAssets.ts`.
